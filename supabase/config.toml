project_id = "spendspeak"

# Google OAuth is enabled in the hosted Supabase Dashboard. For local Supabase
# development, configure [auth.external.google] with client_id and a secret read
# from SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET; never commit the secret.

[functions.parse-transcript]
verify_jwt = true

[functions.delete-account]
verify_jwt = true
